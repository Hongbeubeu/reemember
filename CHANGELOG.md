# Changelog

All notable changes to this project will be documented in this file.

The format is based on Keep a Changelog, and this project currently tracks changes under an `Unreleased` section until version tags are introduced.

## [Unreleased] - 2026-04-22

### Added

- Initial `CHANGELOG.md` for tracking project/documentation changes.

### Changed

- Documentation now reflects current runtime model: desktop-only Tauri app, no standalone CLI mode.
- Documentation now reflects data flow: JSON-only import/export (CSV removed).
- Run instructions standardized to:
  - `cd src-tauri`
  - `cargo run`
- Technical documentation updated to match current modules, commands, dependencies, and Rust edition (`2024`).

### Notes

- Future entries should be based on implemented behavior in source code (not planned behavior).

